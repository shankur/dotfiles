# Dotfiles
### Pre-requisite
* Make sure gcc version > 10.0.0 is installed and properly linked
* Make sure zsh version > 5.9 is installed
### Installation
* `git clone https://github.com/shankur/dotfiles.git ~/.local/share/chezmoi`
* `cd ~/.local/share/chezmoi`
* `./install.sh`
* `chezmoi apply`
